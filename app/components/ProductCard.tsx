import Image from "next/image";
import type { Product } from "../data/products";
export function ProductCard({ product }: { product: Product }) { return <article className="group overflow-hidden rounded-2xl border border-slate-200/70 bg-white transition duration-300 hover:-translate-y-1 hover:shadow-[0_20px_45px_rgba(13,58,92,.12)]"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100"><Image fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" src={product.image} alt={product.name} className="object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-5 sm:p-6"><p className="text-[11px] font-bold uppercase tracking-[.18em] text-brand">{product.category}</p><h3 className="mt-2 text-lg font-bold text-ink">{product.name}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{product.description}</p><a href="/lien-he" className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-brand">Xem chi tiết <span className="transition-transform group-hover:translate-x-1">→</span></a></div></article>; }
